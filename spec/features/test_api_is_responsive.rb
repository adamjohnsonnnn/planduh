require "rails_helper"

feature 'requesting an itinerary recommendation' do
  context 'when user submits parameters for search' do
    scenerio 'when user clicks on button' do
      YelpResponse.create(:message, 10)


    end
  end
end

{ user: user_id }

